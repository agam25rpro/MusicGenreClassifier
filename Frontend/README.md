# Music Classifier Frontend

This folder handles the web-based User Interface for the Music Genre Classifier and is designed to be hosted on Vercel.

It communicates with the Hugging Face Space FastAPI backend (`/api/classify`) to process user audio uploads and present the results completely distinct from the Gradio interface.
